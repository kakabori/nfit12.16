Clone a repository: Will create a directory named after the repo's name
git clone https://github.com/kakabori/nfit12.16.git

git push -u origin master

git push origin

Make the better branch the master branch
git checkout better_branch
git merge --strategy=ours master    # keep the content of this branch, but record a merge
git checkout master
git merge better_branch             # fast-forward master up to the merge

